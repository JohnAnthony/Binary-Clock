Usage flags:
    -h          Display (this) help text
    -c          Colour on (default)
    -nc         Colour off
    -l          Labels on (default)
    -nl         Labels off
    -b          Braces on (default)
    -nb         Braces off
    -t #        Set theme number (1-4)
                (1 is default)
